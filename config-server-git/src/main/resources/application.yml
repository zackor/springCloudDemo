spring:
  application:
    name: config-server
  cloud:
    config:
      server:
        git:
          uri: https://github.com/zackor/springCloudDemo
          search-paths: config-repo

server:
  port: 1201